<div class="single-event-img">
  <img class="event-img" src="{{eventData.pictures.path}}" alt="{{eventData.pictures.name}}"></img>
</div>

<div class="container">
  <div class="event-single">
    <h1>{{eventData.title}}</h1>
    <div class="practical">
      <p>{{eventData.date}}</p>
      <p>{{eventData.time}}</p>
      <p>{{eventData.city}}</p>
      <p>{{eventData.address}}</p>
    </div>

    <div class="info">
      <h2>What we're changing</h2>
      <p>{{eventData.description}}</p>

      <h2>What we'll need</h2>
      <p>{{eventData.needs.need_desc}}</p>
      <p>We need at least {{eventData.needs.amount_of_ppl}} Pairs of helping hands!</p>
      {{#if eventData.needs.car}}
      <p>We will also need a car.</p>
      {{/if}}
    </div>

    <div class="interests">
      {{#if user.interests.interest_educational}}
      <span>Education</span>
      {{/if}} {{#if user.interests.interest_politics}}
      <span>Politics</span>
      {{/if}} {{#if user.interests.interest_artistical}}
      <span>Art</span>
      {{/if}} {{#if user.interests.interest_lgbt}}
      <span>LGBTQ</span>
      {{/if}} {{#if user.interests.interest_local}}
      <span>Local Community</span>
      {{/if}} {{#if user.interests.interest_charity}}
      <span>Charity</span>
      {{/if}} {{#if user.interests.interest_sports}}
      <span>Sports</span>
      {{/if}}
    </div>

    <div class="container-participants">
      {{#each particArr}}
      <div class="{{this.role}}">
        <a href="/user-profile/{{this._id}}"><img src="{{this.picture}}" alt="profile-pic" id="user-pic-round"></a>
        <p>{{this.name}}</p>
      </div>
      {{/each}}
    </div>
    <form action="/events/join/{{eventData.id}}" method="post">
      <button type="submit">Join event!</button>
    </form>
    <form action="/events/leave/{{eventData.id}}" method="post">
      <button type="submit">Leave</button>
    </form>
  </div>
</div>
<div class="map" id="map-single"></div>
<script type="text/javascript" src="/javascripts/singleMap.js"></script>
<script>
  var data = {{{ json eventData }}}
  startMap(data)
</script>